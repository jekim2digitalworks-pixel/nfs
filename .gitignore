# ── Node / pnpm ────────────────────────────────
node_modules/
.pnpm-store/
*.tsbuildinfo

# ── Next.js ────────────────────────────────────
.next/
out/
build/
dist/
.vercel/

# ── Prisma ─────────────────────────────────────
# 생성된 클라이언트는 커밋하지 않는다. 마이그레이션 SQL 은 반드시 커밋한다
packages/db/generated/
!packages/db/prisma/migrations/

# ── 테스트 · 커버리지 ─────────────────────────
coverage/
.vitest/

# ── IDE ────────────────────────────────────────
.idea/
*.iml
.vscode/

# ── 로컬 설정 · 비밀 ──────────────────────────
# 구글 OAuth 클라이언트 시크릿 · Supabase 커넥션 문자열 · CRON_SECRET 은 절대 커밋하지 않는다
.env
.env.*
!.env.example
*.pem
*.key

# ── OS ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 로그 · 임시 ───────────────────────────────
logs/
*.log
tmp/
