# @nfs/web

NFS 화면 + API (Next.js App Router 단일 앱).

문서는 저장소 루트의 [`docs/`](../../docs) 에 있다. 시작점은 [`docs/wbs/wbs.md`](../../docs/wbs/wbs.md).

| 알고 싶은 것 | 문서 |
|---|---|
| 지금 뭘 해야 하나 | `docs/wbs/wbs.md` 🔴 |
| 서버 구조 · 배치 · 시간 처리 | `docs/개발/01-아키텍처.md` |
| 마크업 · CSS · 타이머 | `docs/퍼블/01-퍼블리싱가이드.md` |

```
pnpm dev        # 저장소 루트에서 실행한다 (3000)
pnpm build
pnpm lint
```

> ⚠️ `AGENTS.md` 는 `next dev` 가 자동 생성·갱신한다. 손으로 지우지 않는다.
