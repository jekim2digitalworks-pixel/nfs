/**
 * `server-only` 패키지는 클라이언트 번들에 서버 코드가 섞이면 **빌드를 깨뜨린다.**
 * 그 방어는 프로덕션에서 그대로 살아 있어야 한다 (아키텍처 §2.2).
 *
 * 다만 Vitest 는 번들러가 아니라 Node 에서 모듈을 그냥 import 하므로
 * 이 패키지가 무조건 예외를 던진다. 테스트 러너에서만 빈 모듈로 바꾼다.
 */
export {};
